{%- assign jsLogo = settings.logo -%}

<!-- CSS link -->
{{ 'js-hero.css' | asset_url | stylesheet_tag }}

<!-- GSAP file -->
<script src="{{ 'js-hero-gsap.js' | asset_url }}" defer="defer"></script>

<section class="js-hero-section">
  <div>
    <h1>conteudo aqui</h1>
  </div>
</section>

{% schema %}
{
  "name": "JS Hero Shapes",
  "max_blocks": 5,
  "settings": [
    {
      "type": "color",
      "id": "bg_color",
      "label": "Backgorund Color",
      "default": "#000000"
    }
  ],
  "blocks": [
    {
      "type": "shape",
      "name": "Shape Irregular",
      "settings": [
        {
          "type": "image_picker",
          "id": "shape_img",
          "label": "Imagem do Shape"
        },
        {
          "type": "color",
          "id": "shape_bg",
          "label": "Shape's Background Color",
          "default": "#cccccc"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "JS Hero Shapes",
      "blocks": [
        {
          "type": "shape"
        },
        {
          "type": "shape"
        },
        {
          "type": "shape"
        },
        {
          "type": "shape"
        },
        {
          "type": "shape"
        }
      ]
    }
  ]
}
{% endschema %}
